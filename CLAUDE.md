# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code (claude.ai/code) へのガイダンスです。

## プロジェクト概要

task-board: タスク管理ボードアプリケーション。

- 技術スタック: 未定（決まったらここに追記する。例: フレームワーク、言語、DB など）
- ディレクトリ構成: 未定（プロジェクトが立ち上がったらここに追記する）

## 開発コマンド

技術スタックが決まっていないため、コマンドは未定義です。決まったら以下の項目を埋めてください。

- セットアップ: TODO
- 開発サーバー起動: TODO
- ビルド: TODO
- テスト実行: TODO
- Lint / フォーマット: TODO

## Git運用ルール

**コードに変更を加えたら、その都度コミットし、GitHubへpushすること。**
作業を溜め込んでまとめてpushするのではなく、意味のある変更の単位（1つの修正・1つの機能追加など）ごとに以下を実施する。

1. 変更内容を確認する（`git status`, `git diff`）
2. 意味のある単位でコミットする（コミットメッセージは変更内容が分かるように日本語または英語で簡潔に記述）
3. 直ちに `git push` してGitHub上のリモートリポジトリへ反映する
4. pushが完了するまで、その変更を「完了」として報告しない

### ブランチ運用

- 既定ブランチ（`main` など）に直接コミットしてよいか、featureブランチを切るかはプロジェクト方針が決まったら追記する
- 現時点のルール: 特に指示がない限り、作業前に現在のブランチを確認し、既定ブランチ上で直接大きな変更を進めない

### コミットメッセージ

- 変更の目的・内容が分かる一文を先頭に書く
- 複数の変更が混在する場合はコミットを分割する

### その他の注意事項

- `git push --force` やコミット履歴を書き換える操作は、明確な指示がない限り行わない
- pushに失敗した場合は原因を確認し、状況をそのまま報告する（失敗を隠して「完了」としない）

## 現在の状態（メモ）

- このディレクトリはまだ Git リポジトリとして初期化されていません（`git init` 未実施）。
- GitHub上のリモートリポジトリも未設定です。
- 上記の「Git運用ルール」を実際に機能させるには、`git init` とGitHubリモートの設定（`git remote add origin <URL>` など）を先に行う必要があります。
